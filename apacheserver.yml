---
- hosts: all
  become: yes
  tasks:
  - name: I want to install apache2 webserver on ubuntu ansible nodes
    apt:
      name: apache2
      state: present
  - name:
    service:
      name: apache2
      state: started